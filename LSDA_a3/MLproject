name: A3

conda_env: A3.yaml

mlflow server \
    --backend-store-uri sqlite:///mlflow.db \
    --default-artifact-root ./artifacts \
    --host 0.0.0.0

entry_points:
  main:
    command: "python LSDA_as3_template.py"
